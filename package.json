{
  "scripts": {
    "dev": "wrangler dev src/index.ts",
    "build:fe": "esbuild src/client/index.js --bundle --minify --sourcemap --outfile=assets/bundle.js",
    "deploy": "wrangler deploy --minify src/index.ts"
  },
  "dependencies": {
    "@hotwired/stimulus": "^3.2.2",
    "hono": "^3.11.11"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231218.0",
    "esbuild": "0.19.10",
    "wrangler": "^3.22.1"
  }
}
